mixin radio-button(text, nameText, valueText)
    label.radio-button&attributes(attributes)
        input.radio-button__input(type="radio" name=nameText value=valueText)
        .radio-button__elem
        .radio-button__text= text